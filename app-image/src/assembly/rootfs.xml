<assembly xmlns="http://maven.apache.org/ASSEMBLY/2.1.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xsi:schemaLocation="http://maven.apache.org/ASSEMBLY/2.1.0 http://maven.apache.org/xsd/assembly-2.1.0.xsd">
    <id>rootfs</id>
    <formats>
        <format>tar</format>
    </formats>
    <includeBaseDirectory>false</includeBaseDirectory>
    <fileSets>
        <!--
            Directories may have permissions, owner and group in TAR only if they are added as TAR entries.
            Below file set adds emtpy directories explicitly to ensure that TAR directory entries are created.
        -->
        <fileSet>
            <!--
                This file set adds directory TAR entries corresponding to the following tree:

                deployments (0777 root:root)
            -->
            <outputDirectory/>
            <directory>src/main/resources/rootfs</directory>
            <excludes>
                <exclude>**/*.*</exclude>
            </excludes>
            <includes>
                <include>deployments</include>
            </includes>
            <directoryMode>0777</directoryMode>
            <directoryOwner>
                <user>
                    <name>root</name>
                    <id>0</id>
                </user>
                <group>
                    <name>root</name>
                    <id>0</id>
                </group>
            </directoryOwner>
        </fileSet>
    </fileSets>
    <dependencySets>
        <!--
            If below sets add the same directory(s) as mentioned by previous (see above) file set
            then it is considered as duplicate and does not override permissions, owner and group of directory
            specified in previous file set, i.e. such directory is not added as new TAR directory entry,
            but if directory contains files then new TAR file entries are still created (files are still added).
        -->
        <dependencySet>
            <outputDirectory>deployments</outputDirectory>
            <includes>
                <include>*:ear</include>
            </includes>
            <outputFileNameMapping>${artifact.artifactId}${dashClassifier?}.${artifact.extension}</outputFileNameMapping>
            <useProjectArtifact>false</useProjectArtifact>
            <fileMode>0444</fileMode>
            <fileOwner>
                <user>
                    <name>root</name>
                    <id>0</id>
                </user>
                <group>
                    <name>root</name>
                    <id>0</id>
                </group>
            </fileOwner>
            <directoryOwner>
                <user>
                    <name>root</name>
                    <id>0</id>
                </user>
                <group>
                    <name>root</name>
                    <id>0</id>
                </group>
            </directoryOwner>
        </dependencySet>
    </dependencySets>
</assembly>
